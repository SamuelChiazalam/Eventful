NODE_ENV=development
PORT=5000

# MongoDB Configuration
# Development: localhost
# Production: Use MongoDB Atlas - mongodb+srv://user:pass@cluster.mongodb.net/eventful?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/eventful

# Redis Configuration
# Development: localhost
# Production: Use managed Redis (Render, Redis Labs, etc.)
REDIS_URL=redis://localhost:6379

# JWT Configuration
# Important: Change JWT_SECRET to a strong random string (min 32 chars) in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-at-least-32-chars
JWT_EXPIRY=7d

# Paystack Payment Gateway
# Get from: https://dashboard.paystack.com/settings/api-keys-and-webhooks
# Leave empty for demo mode (no Paystack key required)
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxxxxxx
# Optional: Paystack public key for client-side usage (if needed)
PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxx

# Email Configuration
# For Gmail: Use app-specific password (not your account password)
# Enable "Less secure app access" or use app-specific password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-not-account-password

# Frontend URL (for callbacks and CORS)
# Development: http://localhost:3000
# Production: https://eventful-frontend.onrender.com
FRONTEND_URL=http://localhost:3000

# Optional: API Base URL (used by frontend)
# Development: http://localhost:5000/api
# Production: https://eventful-api.onrender.com/api
# API_BASE_URL=http://localhost:5000/api

# Logging Level (optional)
# Options: error, warn, info, debug, verbose
# LOG_LEVEL=info

# Rate Limiting (optional, in seconds)
# AUTH_RATE_LIMIT_WINDOW=900
# AUTH_RATE_LIMIT_MAX_REQUESTS=5
# API_RATE_LIMIT_WINDOW=900
# API_RATE_LIMIT_MAX_REQUESTS=100
